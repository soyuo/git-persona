{
  "name": "git-persona",
  "version": "1.0.0",
  "description": "A CLI for switching Git identities, credentials, tokens, and SSH profiles without logging out.",
  "license": "MIT",
  "type": "module",
  "author": "soyuo",
  "bin": {
    "git-persona": "./bin/git-persona.js"
  },
  "scripts": {
    "start": "node ./bin/git-persona.js",
    "check": "node ./bin/git-persona.js --help && node ./bin/git-persona.js --version",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
